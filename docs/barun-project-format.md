# `.barun` 프로젝트 형식

`.barun`은 아웃리치 회계 프로젝트 데이터와 영수증·증빙 파일을 한 파일로 이동하기 위한 ZIP 기반 문서 패키지입니다.

## 컨테이너 구조

```text
team-accounting.barun
├── manifest.json
└── attachments/
    ├── receipt-a.heic
    ├── heif-render-a.jpg
    ├── preview-a.jpg
    └── fuel-evidence.png
```

`manifest.json`의 최상위 필드는 다음과 같습니다.

```json
{
  "format": "barun-accounting-project",
  "formatVersion": 1,
  "savedAt": "2026-07-17T00:00:00.000Z",
  "project": {}
}
```

- `formatVersion`은 패키지 컨테이너의 호환성 버전입니다.
- `project`에는 회계 데이터와 각 첨부파일의 원본·렌더링·미리보기 상대 경로, 이미지 편집값이 저장됩니다. 팀별사역지원금으로 사용한 지출은 독립 항목인 `teamMinistry`(6. 팀별사역비)로 저장됩니다. 이미지 편집값에는 자동 흐름 배치를 위한 그림 너비·높이, 원본 비율, 맞춤/채우기 방식과 자르기 확대율·위치·회전값이 포함됩니다. 오프라인 지출과 공통 주유비 산정 증빙에는 여러 개의 실물 부착 공간과 각각의 너비·높이를 저장할 수 있습니다.
- 기기별 절대 작업 경로와 CLOVA Secret Key는 패키지에 포함하지 않습니다.
- 첨부파일은 반드시 `attachments/` 아래에 저장합니다.
- PDF 첨부는 가져오는 즉시 페이지별 PNG로 변환합니다. 프로젝트 manifest와 `attachments/`에는 원본 PDF 대신 변환된 이미지만 포함합니다.
- HEIC·HEIF 첨부는 사용자가 넣은 원본을 그대로 포함하고, 호환성 높은 고화질 JPEG 렌더링본과 편집 화면용 축소 미리보기를 추가합니다. 일반 JPEG·PNG도 크기가 크면 원본과 별도로 축소 미리보기를 만듭니다.
- 편집 화면은 축소 미리보기를 우선 사용하고 PDF·Word 영수증철은 원본 또는 고화질 렌더링본을 사용합니다. 이미지 자르기·크기 조절 값은 원본 기준의 비파괴 편집 정보이므로 미리보기 생성 여부와 무관하게 유지됩니다.
- JPEG·PNG·HEIF 등 이미 압축된 첨부 자산은 ZIP 내부에서 `STORE` 방식으로 저장합니다. 다시 DEFLATE하지 않아도 파일 크기 차이가 거의 없고, 대용량 프로젝트의 저장 시간을 크게 줄일 수 있습니다.
- 저장 시 패키지 전체를 같은 폴더의 임시 파일에 먼저 기록하고 디스크 동기화를 마친 뒤 기존 `.barun`과 교체합니다. 패키지를 열 때 manifest가 참조하는 이미지가 하나라도 빠졌으면 손상된 프로젝트로 보고 열기를 중단합니다.
- 앱 업데이트 직전에는 최신 프로젝트와 동일한 내용의 `프로젝트명-업데이트전-백업.barun`을 같은 폴더에 남깁니다. 이 파일도 일반 `.barun`과 동일하게 직접 열 수 있습니다.
- 앱은 기존 `회계프로젝트.json`을 읽을 수 있지만 새 저장은 `.barun`을 사용합니다.
